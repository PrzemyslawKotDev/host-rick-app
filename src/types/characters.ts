import type InfoInterface from "@/types/info";
import type HeroInterface from "@/types/hero";

type CharactersType = {
    info: InfoInterface;
    results: HeroInterface[];
}

export default CharactersType